import org.junit.Test;
import org.junit.Assert;

public class ConfigurationTest {
    @Test
    public static void testConfigurationTest() {
        Configuration config = new Configuration();
        config.
    }
}
